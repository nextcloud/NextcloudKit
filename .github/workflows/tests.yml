name: Tests

on:
  workflow_call:
    inputs:
      scheme:
        required: true
        type: string
      destination:
        required: true
        type: string

jobs:
  run-tests:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v2
    - name: Test NextcloudKit
      run: |
        xcodebuild test -scheme "${{ inputs.scheme }}" -destination "${{ inputs.destination }}"
    
